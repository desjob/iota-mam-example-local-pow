{
  "name": "mamtest",
  "version": "0.1.0",
  "private": true,
  "homepage":"https://desjob.github.io/iota-mam-example-local-pow/",
  "dependencies": {
    "@iota/mam": "^0.7.2",
    "curl-transaction-core": "^1.0.3",
    "curl-transaction-webgl2-impl": "^1.0.2",
    "gh-pages": "^2.0.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
